# Domain model

**Status:** planned

Domain entities and their relationships: network element, link, service class, fault, incident, diagnosis, action, plan, run. Defines the vocabulary shared by every experiment so that two experiments naming the same concept mean the same thing.
